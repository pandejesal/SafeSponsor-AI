# Audit — 2026-08-16

Audit of SafeSponsor AI at `8b6cd76` (fixes through `6517b60`). Sources: 3 parallel review lanes (payments, security, SEO) + live E2E against `https://safe-sponsor-ai.vercel.app` (drivers `.swarm/e2e/e2e5.js`).

## Critical

| ID | Finding | Status |
|---|---|---|
| E2E-1 | **Google sign-in could not complete on prod.** Both redirect AND popup flows route through the Firebase hosted auth handler (`authDomain/__/auth/handler`), which bootstraps by fetching `/__/firebase/init.json` — 404 for projects with no Firebase Hosting site (app runs on Vercel). Three E2E attempts returned signed-out. | ✅ **RESOLVED** — Firebase Hosting enabled (default site `safesponsor-ai-958cd`, placeholder at `hosting/public/index.html`, deployed via `firebase-tools` CLI). `__/firebase/init.json` now serves 200 on the authDomain; popup sign-in completes; session persists across runs. |

## High-impact defects found by E2E (all fixed)

| ID | Sev | Finding | Fix |
|---|---|---|---|
| E2E-2 | HIGH | **Client-side Firestore is impossible on this project: App Check is enforced on Firestore, and the client SDK's streaming Listen channel attaches no App Check token.** Every dashboard load logged `Firestore history snapshot error: Missing or insufficient permissions` for `onSnapshot(users/{uid}/history)` — regardless of security rules (verified by REST probe: 403 without `X-Firebase-AppCheck`, 200 with it; rules deploys had zero effect on the requests). | ✅ `6517b60` — new `GET /api/history` reads via Admin SDK; dashboard fetches it; client `getFirestore` removed from `lib/firebase.ts` (smaller bundle, no dead channel). Rules restored to the locked-down server-only ruleset. |
| E2E-3 | MED | CSP `connect-src` lacked `https://firestore.googleapis.com` → client Firestore stream blocked (only visible once the listener bug was fixed; also made E2E-2's errors look like network failures). | ✅ `80f937f` — host added to CSP. |
| E2E-4 | MED | **Dodo product prices do not match the UI.** Checkout cart quotes (test mode, IDR-denominated totals from USD-configured products): Single Report 18,549,443 (UI: $8), Channel Report 46,373,608 (UI: $19), Pro (product has NO name) 369,133,925 (UI: $149). | ✅ **FIXED + VERIFIED ON PRODUCTION** — old Dodo account lost; user created a new one and supplied a products-scoped test key. Products recreated programmatically at correct USD prices, Pro named (ops-runbooks §7). Webhook endpoint + signing secret wired. Vercel CLI installed, env vars replaced (scripts/vercel-env-sync.js), production redeployed (`dpl_5VWu51mv5MYJ2rKGnjc8YjShP5S7`). Verified with a real authenticated session (profile re-seeded via `.swarm/e2e/reseed.js`): `POST /api/checkout` for all 3 plans → 200 + `test.checkout.dodopayments.com/session/cks_…` (`.swarm/e2e/checkout-api-check.js`). E2E5 run: 7 PASS / 0 FAIL (H1/H2/H4/T1/T2/A1/A2), S1/C1/P1 card clicks partial (driver timing noise; same selectors passed pre-cleanup). |
| E2E-5 | LOW | E2E listener bug: `ctx.on('page')` never attached to the context's first page → earlier runs' A1/A2 verdicts were empty (false green). | ✅ fixed in `e2e5.js` (attach directly + scope console to app origin). |

## Security lane

| ID | Sev | Finding | Fix |
|---|---|---|---|
| SEC-H1 | HIGH | App Check gate dead code: `if (!appCheckOk)` (object always truthy) on takedown POST, takedown admin, takedown `[id]` GET+POST, export — enforcement never fired even with `ENFORCE_APP_CHECK=true`. | ✅ `!appCheckOk.valid` on all 5 sites (`d1d69ab`) |
| SEC-M2 | MED | verify-payment reads body unbounded (no 1MB cap). | ✅ 413 cap added |
| SEC-M3 | MED | Rate limiting only on /api/analyze; verify-payment/checkout/takedown/save-dossier/export unthrottled. | ✅ 20/min transactional limit on verify-payment; others documented |
| SEC-L4 | LOW | Webhook idempotency fallback `Date.now()` — retries non-idempotent. | ✅ webhook-id now required (401 when missing) |
| SEC-L5 | LOW | Takedown dedupe check-then-add race → duplicate pending requests. | ✅ atomic transaction |
| SEC-L6 | LOW | clear-cache unguarded `request.json()` → 500 on malformed JSON. | ✅ 400 |
| SEC-L7 | LOW | console.log leaks uids/paymentIds/balances on webhook, verify-payment, checkout, cancel-subscription. | ✅ uids redacted |

## Payments lane

| ID | Sev | Finding | Fix |
|---|---|---|---|
| P-M1 | MED | Webhook idempotency record committed BEFORE the `!uid` bail-out and never deleted → unresolvable-uid events permanently lost (Dodo never retries). | ✅ uid check moved before idempotency write |
| P-M2 | MED | verify-payment grants ANY succeeded payment in last 30 days without refund check — refunded payment re-claimed once `lastPaymentId` advances. | ✅ `refunded_payments/{paymentId}` deny-list written on `refund.succeeded`; verify-payment refuses (410) |
| P-M3 | MED | Intro $99 discount re-applied via 30-min TTL self-heal → two discounted months. | ✅ `introPending` blocks re-application until grant clears it |
| P-M4 | MED | verify-payment subscription grant resets `expiresAt = now+1mo`, SHORTENING a webhook-extended subscription. | ✅ max(existing, now) + 20-day period guard (webhook parity) |
| P-M5 | MED | `lastSubscriptionId` never set on verify-payment first grant → renewal payloads can't resolve uid. | ✅ set from `matchingPayment.subscription_id` |
| P-LOW | LOW | `reportCredits` never granted by any payment path (dead entitlement). | documented |
| P-LOW | LOW | `refund.succeeded` without subscription context defaults to plan "single" → decrements a legitimate video credit (clamped). | documented — payload-shape dependent |

## SEO / copy lane

| ID | Sev | Finding | Fix |
|---|---|---|---|
| S-001 | MED | Terms §2/§7 reference "Section 11" for contact; contact is §12. | ✅ §12 |
| S-002 | MED | Terms claim test-mode-only payments, but checkout is env-dependent with no UI disclosure. | ✅ `TestModeBadge` on home pricing + dashboard via `/api/health` `paymentsMode`; hides once live |
| S-003 | MED | Prices hardcoded in UI/JSON-LD; no verification against Dodo product config. | **CONFIRMED REAL (E2E-4)** — now resolved via the new Dodo account: products exist at $8/$19/$149 USD matching the UI (see E2E-4 row). Remaining gap: UI still never verifies prices at runtime; acceptable while product IDs are pinned to env vars. |
| S-004 | LOW | "7-Day Cache" claim vs 90/180-day implementation + privacy policy. | ✅ "90-Day Cache" |
| S-005 | LOW | JSON-LD description names only YouTube & Instagram; product covers 4 platforms. | ✅ all 4 |
| S-006 | LOW | $99 intro banner assumes the discount code is exactly $50 off, unverified. | documented — verify code amount in Dodo dashboard |

## E2E results (prod, final run `run5e` — 10/10 PASS)

- **H1** PASS — title/hydration on prod.
- **H2** PASS — teaser CTA → `/dashboard` (persisted session path).
- **H4** PASS — signed-in state confirmed via `/api/check-credits`.
- **T1** PASS — hero teaser fires `/api/analyze`; `freeTeaserUsed` flips false→true server-side; "Scanning Creator" state observed.
- **T2** PASS — audit history loads via `/api/history` (no client Firestore denial).
- **S1/C1/P1** PASS — all three plan cards reach Dodo test checkout (`test.checkout.dodopayments.com/session/cks_…`).
- **A1/A2** PASS — zero console errors / failed requests on the app origin (Firestore denials gone; Dodo-page artifacts excluded).

## Payment E2E (prod test mode, `.swarm/e2e/pay-e2e.js` — 9/9 PASS)

Real end-to-end payments through the production Vercel app against the new Dodo test account (Airwallex-based hosted checkout). Test user `BawpZULCjAOko5NEdIEGrustnYm1`, card success `4242 4242 4242 4242` (06/32, 123).

- **S1/S3** PASS — `/api/checkout` single → 200 test session; after paying, Dodo redirects to its result page (`test.checkout.dodopayments.com/{token}?session=…`).
- **S2** PASS — webhook grant: `videoCredits +1` for the single purchase (delta-based assertion; observed on every run).
- **P1/P3** PASS — `/api/checkout` subscription → 200 test session; Pro $149.00/Month shown on the Dodo page; payment completes.
- **P2** PASS — no double-grant: with an active subscription >20 days out, a repeat Pro purchase updates `plan` but does NOT extend `expiresAt` (webhook/route.ts:306 guard; also verified in `verify-payment` route.ts:161). Anti-fraud behavior confirmed live.
- **C1/C2** PASS — `/api/cancel-subscription` returns 200 on a cancellable subscription (verified in an earlier run: `success:true`, `expiresAt` preserved); returns 400 "already scheduled for cancellation" when re-called (correct idempotent behavior). `cancelAtPeriodEnd=true` reflected via `/api/check-credits`.
- **D1** PASS — declined card `4000 0000 0000 9995`: credits unchanged (no webhook grant). Sandbox note: Dodo's test decline cards are NONDETERMINISTIC on this Airwallex-based checkout — `4000000000000002` and `4000000000000119` occasionally complete as successes (a webhook grant then follows correctly, because the app grants exactly when Dodo reports `payment.succeeded`). `9995` shows no-grant in the majority of attempts (6/7 observed); the suite retries up to 3 attempts. The app never granted without a success webhook in any run.
- **Verify-payment note** — the dashboard toast "Payment verified!" after a `dodo_success=true` redirect can match an OLD succeeded payment from the 30-day list (returns "already granted" — safe, no double-grant, `lastPaymentId` guard). Cosmetic only in re-verify scenarios.

Checkout-flow facts for future runs: contact form uses Google Places autocomplete (click "Enter manually"); country select `select[name="country"]`; card fields load asynchronously in `sdk-v2.custom.hs.dodopayments.com` iframe (poll up to 30s); "Pay in USD/EUR" is the currency toggle, not the pay button. Profile locale may flip the displayed currency (EUR+VAT) — amounts shown are Dodo-side conversions and do not affect grants (product-ID based).

## Deployments this round

- `b08096f` popup sign-in · `d1d69ab` audit fix batch · `b6c5697` honest monetization + audit report · `80f937f` CSP Firestore · `6517b60` server-side history + rules restore · `df5f6a1` Dodo products+webhook on new account. Firebase: Hosting site + locked-down `firestore.rules` released via CLI (`firebase.json` at root pins the rules file). Vercel: production redeploy `dpl_5VWu51mv5MYJ2rKGnjc8YjShP5S7` via CLI (Dodo env vars replaced).
- `63bb3e2` docs (payment E2E 9/9 section, open item 6, deployments line) + `scripts/vercel-env-sync.js` SYNC_TARGET (preview) support.
- `bd71f63` CSP: allow `https://*.ingest.sentry.io` + `https://*.ingest.us.sentry.io` in `connect-src` (Sentry US-region ingest; note `*.ingest.sentry.io` does NOT cover the `*.ingest.us.*` host).
- **Sentry live (2026-08-17)**: DSNs added to Vercel (Production + Preview, `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN`); deploys `dpl_HANFVxnC31xpRj4GjLUQjFokmzmc` (DSN first), `dpl_FWUoESpxQwyptLG3dnv4HAY4cZm2` (CSP fix), `dpl_5g7jLo8jr4MK1wtBhRfumD8ppGVj` (CSP region fix). Verified end-to-end via CDP-level network capture: client SDK inits with the DSN inlined, a real thrown `sentry-e2e-exception-*` was captured and delivered to `o4511926659973121.ingest.us.sentry.io` with HTTP 200 (4 envelope POSTs, all 200). No CSP violations remain. Check https://sentry.io/issues for the probe events.

## Open items (user actions)

1. **Discount code + live-mode prep**: verify the intro discount code is exactly $50 off (S-006) in the Dodo dashboard (`DODO_PAYMENTS_DISCOUNT_CODE_PRO_INTRO` is unset everywhere → intro currently NOT offered; fail-open to full $149). **Before live go-live**: repeat the Dodo setup in LIVE mode (key, product IDs, webhook secret) and re-run `scripts/vercel-env-sync.js` + `vercel --prod --yes`.
2. Firebase Auth authorized domains: `safe-sponsor-ai.vercel.app` — sign-in works (E2E run6 10/10 PASS), so present in effect; visual check only (Firebase console → Authentication → Settings → Authorized domains).
3. Restore drill: still pending — needs gcloud + a service-account JSON (neither present on the dev machine). Runbook: `docs/ops-runbooks.md` §2.
4. UptimeRobot monitor — not configured yet (needs the user's UptimeRobot account; two HTTP monitors: homepage + `/api/health`, keyword `ok`, 5-min interval). Sentry: ✅ configured + verified (see Deployments; dashboard confirmation at https://sentry.io/issues). ✅ `safesponsor-ai2` Vercel project removed 2026-08-16 via CLI. Runbooks: `docs/ops-runbooks.md` §3–§5.
5. E2E hygiene: ⏳ partially done — `.swarm/e2e/` cleaned 2026-08-16 (`.creds`, `profile/`, `out*`, one-off scripts deleted; `e2e5.js` kept as the canonical driver). Password rotation + prod test-record cleanup (teaser flag, history) are scripted at `scripts/rotate-test-password.js` but need the real `FIREBASE_SERVICE_ACCOUNT` JSON (local env files carry the redacted `[SENSITIVE]` placeholder). Runbook: `docs/ops-runbooks.md` §1.
6. Payment E2E drivers live at `.swarm/e2e/pay-e2e.js` (+ probe scripts); test user carries test-mode entitlements (credits, a cancelled-at-period-end Pro sub expiring 2026-09-17) — harmless test data, resettable only via Admin SDK when the service-account JSON is available.

